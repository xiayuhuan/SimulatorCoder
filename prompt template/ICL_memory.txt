prompt = "You are an experienced DNN accelerator simulator designer and Python language expert."
Please generate new code based on the following example:

==== ICL Example ====
Task Description
Function: manage_prefetches
Purpose: Manage memory prefetch operations, handle address accesses, and maintain buffer state
Inputs:
  - cycle (int): Current simulation cycle
  - addr (int): Memory address to access
Actions:
  1. First check if address exists in buffer (hit check)
  2. If miss occurs:
     a. Add address to current set
     b. Increment element counter
  3. When set reaches capacity threshold:
     a. Add current set to prefetch list
     b. Reset current set
     c. Update set ID
  4. When set ID reaches prefetch trigger point:
     a. Calculate required bandwidth
     b. Execute prefetch
  5. Update cycle tracking variables for prefetch
Output: No return value (updates internal state)
Code Implementation
def manage_prefetches(self, cycle: int, addr: int) -> None:
    if self.check_hit(addr):  # Hit check
        return
    
    if addr not in self.current_set:
        self.current_set.add(addr)
        self.elems_current_set += 1
        
        if self.elems_current_set == self.items_per_set:  # Set full condition
            self.commit_current_set()
            
            if self.need_prefetch():  # Check prefetch trigger
                self.execute_prefetch(cycle)

def commit_current_set(self):
    """Commit the current set and reset tracking variables"""
    self.list_of_sets.append(self.current_set)
    self.current_set = set()
    self.elems_current_set = 0
    self.current_set_id += 1
==== Your Task ====
You are an experienced DNN accelerator simulator designer and Python language expert. Please answer the questions in a professional style: Based on the files you wrote before (and the files sent to you), please write a Python method, just output the code. The following is a functional description of the file code:
Please answer the questions in a professional style: Based on the files you wrote before (and the files sent to you), please write a Python method. Let's think step by step. You should first write a rough problem-solving process using three programming structures(i.e.,sequential,branch,andloop structures)and then output the final code. No try-exception structure, The following is a functional description of the file code:

Function: xxx
Purpose: xxx
Actions:xxx
Output:xxx